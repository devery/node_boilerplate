export const regExps = {
	ONLY_VOWELS_REGEX: /([aeyiuo]+)/gi,
	ONLY_DIGITS_REGEX: /([\d]+)/g,
	NOT_DIGITS_LETTERS_REGEX: /[^a-z\d]+/gi
};

export const BUY_VIEW_TYPES = {
	ACTIVE: 'Active',
	STATISTICS: 'Statistics'
};
