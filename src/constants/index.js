import * as confirmationTypes from './confirmationTypes'
import * as confirmationStatuses from './confirmationStatuses'
import * as emailStatuses from './emailStatuses'
import * as errors from './errors'
import * as events from './events'
import * as providers from './providers'
import * as roles from './roles'

export default {
  confirmationTypes,
  confirmationStatuses,
  emailStatuses,
  errors,
  events,
  providers,
  roles,
}
